<% if (attribute.type === 'string') { %>
        <%- key %>: {
          type: 'string',
          props: {
            floatingLabelText: '<%- _.capitalize(key) %>',
            style: { width: '100%' },
            name: '<%- key %>'
          }
        }<% if(!last) { %><%- ',' %><% } %>
<% } else if (attribute.type === 'text') { %>
        <%- key %>: {
          type: 'text',
          props: {
            floatingLabelText: '<%- _.capitalize(key) %>',
            style: { width: '100%' },
            name: '<%- key %>',
            multiLine: true
          }
        }<% if(!last) { %><%- ',' %><% } %>
<% } else { %>
        <%- key %>: {
          type: attribute.type,
          props: {}
        }<% if(!last) { %><%- ',' %><% } %>
<% } %>